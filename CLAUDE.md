# CLAUDE.md

## このプロジェクトについて
（目的未設定。このファイルを編集して目的を記載してください）

## 開発ルール
- 出力・コメントは日本語で統一する

## ツール実行の許可ルール
通常のBashコマンドは確認なしで実行する。
以下の操作は実行前にユーザーに確認すること：
- rm -rf などの復元不可能な削除
- git push --force
- sudo（apt install 以外）
- 外部への公開・デプロイ操作
